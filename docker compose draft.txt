
version: "3.9"
services:
  db:
    build: .
    ports:
      - "5000:5000"
  redis:
    image: "redis:alpine"

  web: (liefert html und spiellogik)
    image: node
    ports: 80

  lb:
    image: traefik:2.4
    --ports:
      -- 80:81
    --volumes:
      -- "./traefik.toml:/traefik.toml"
      -- "./traefik_dynamic.toml:/traefik_dynamic.toml"
    --depends_on:
      -- service
    --networks:
      -- internal
      -- external




 
	

	